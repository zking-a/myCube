# Light Games UI Style Guide

## 目标与主视觉

本项目是面向短局、轻度竞技与脑力挑战的多游戏站。唯一的视觉方向为 **轻竞技桌游（Light Competitive Tabletop）**：清爽的蓝紫作为系统与行动主色，暖橙用于题目、进度和注意力锚点；棋盘、数字牌与棋子承担玩法辨识，而不是用网页仪表盘式卡片取代游戏物件。

保留现有的明亮背景、蓝橙对比和细腻层级；减少无意义的圆角、玻璃模糊、渐变和悬浮阴影。每一页应优先呈现“这一局该做什么”，再呈现状态与辅助操作。

## 视觉审计（2026-08-22）

### 项目现状

- 页面：游戏平台首页；24 点的首页、选关、闯关、竞速、联机房间、对局与结算；数独大厅、棋局、统计/技巧/分享弹窗；中国跳棋大厅、棋局、联机等待与胜利弹窗。
- 共享实现：目前没有可被三款游戏共同加载的 Token 或组件样式。24 点样式内联且同时保留旧版和“视觉升级”两套规则；数独、跳棋和平台各自维护颜色、间距、圆角与阴影。
- 资源：未发现图片、字体、图标包、Shader 或 UI 特效资源；关键图标目前混用 Emoji、Unicode 符号与 CSS/SVG 棋子。
- 框架：原生 HTML/CSS/JavaScript，由 Node 静态托管；没有第三方 UI 组件库。

### A — 已一致，保持

- 游戏平台首页的总体色彩、字体和入口层级。
- 24 点首页的蓝/橙/绿模式分层，以及数字牌作为核心交互物件的表达。
- 24 点首页的“高权重模式入口 → 按需显示配置”流程，是其他小游戏大厅的入口模板；中国跳棋与数独已据此完成迁移。
- 所有好友联机入口统一为“创建房间 / 加入朋友房间”的意图选择；只在加入流程显示房间码，邀请链接则直接打开对应的加入或房间状态。
- 中国跳棋的红蓝棋子、回合状态、棋盘落点和路线反馈。

### B — 小幅调整即可

- 数独大厅与棋局：大厅已调整为紧凑英雄区、模式入口与按需展开的难度配置；棋局和深色主题仍需逐步映射到相同的语义 Token。
- 三款游戏的导航、辅助按钮、输入框、提示条、进度条与弹窗：主要是边框、间距、状态和焦点样式需要收敛。

### C — 存在明显风格漂移，需要分阶段整理

- 24 点的选关、联机、比较和结算状态：旧样式与升级样式在同一文件中叠加，颜色和圆角来源分散。
- 所有 Emoji 图标：插画性 Emoji 与数独/跳棋的几何游戏物件在关键操作中混用，跨平台外观不稳定。
- 无需为当前大厅再做结构级重写；后续只针对单个对局页的状态密度与辅助操作做微调。

### D — 需要重做

- 当前没有必须在第一阶段重做的页面。先完成基础收敛，再根据截图选择单个高价值页面进行重构。

## Design Tokens

唯一来源：`public/ui-foundation.css`。页面样式应以 `--ui-*` Token 映射旧变量，禁止为通用语义新增裸 HEX。

| 语义 | Token | 值 |
| --- | --- | --- |
| 页面底色 | `--ui-canvas` | `#EEF3FF` |
| 温暖背景收束 | `--ui-canvas-warm` | `#FFF7EF` |
| 主表面 / 浮层 | `--ui-surface` / `--ui-surface-raised` | `#FFF` / 白色 94% |
| 默认 / 强边框 | `--ui-border` / `--ui-border-strong` | `#E1E7F3` / `#CFD9EE` |
| 主行动 / 强态 | `--ui-primary` / `--ui-primary-strong` | `#5668F5` / `#3347D8` |
| 题目、进度、强调 | `--ui-accent` | `#FF8A4C` |
| 成功 / 警告 / 危险 | `--ui-success` / `--ui-warning` / `--ui-danger` | `#18AD78` / `#F6C84C` / `#E85D64` |
| 主、次、弱文本 | `--ui-text` / `--ui-text-secondary` / `--ui-text-muted` | `#18233D` / `#71809A` / `#94A3B8` |

间距只使用 4、8、12、16、24、32、48px（`--ui-space-1` 至 `--ui-space-7`）。

## 字体与数字

- 字体：`--ui-font-sans`；不引入未加载的装饰字体。
- 标题：Display 32/40、H1 28/34、H2 22/28、H3 18/24，权重 800–900。
- 正文：14/22；辅助文案 12/18；标签最小 11/16。不要在常规桌面 UI 使用小于 11px 的文本。
- 计时、分数、房间码和棋盘数字使用 `--ui-font-number` 与 `font-variant-numeric: tabular-nums`。

## 菜单命名

- 顶层入口采用玩法名，不用泛化的“模式”：`闯关挑战`、`竞速挑战`、`人机对战`、`本地双人`、`好友对战`。
- 数独只有单人棋局状态，使用动作名：`继续上局`、`开始新局`；难度属于下一层配置，不作为顶层玩法名称。
- 联机动作固定为：`创建房间`、`加入朋友房间`、`对战房间`、`房间码`。数独双人同盘固定使用 `好友协作`，不与 `好友对战` 混用。

### 数独好友协作

- `好友协作`是独立入口：创建者选择难度并生成邀请链接，加入者输入房间码或打开邀请链接；不覆盖本地单人存档。
- 双方填写同一份服务端盘面，协作状态条固定展示房间码、在线状态与“复制邀请 / 暂离”。候选笔记保留为个人辅助，避免两人互相擦写思路。
- 暂离只断开当前连接，凭私密重连身份可回到原来的盘面；完成后显示“共同完成”，不伪装为胜负对战。

## 跳棋人机与棋盘反馈

- `简单`保留随机落子，用于轻松体验；`标准`使用两层受预算的 DFS 对弈搜索；`困难`使用三层 DFS、alpha-beta 剪枝与固定节点上限。难度差异应体现在判断质量，不能以延长等待时间伪装难度。
- 局面评估综合已进入目标营地的棋子数、前进进度与距离目标营地的远近；合法走法仍完全由规则核心生成，联机服务端的判定不受人机策略影响。
- 棋盘以温暖桌面、凹槽落点与低饱和目标营地标识来体现方向；当前可走点、上一步路径与棋子本身保持高对比，避免把提示压过实际棋局。

## 形状、材质与特效

- 游戏物件：数独格与棋盘可使用 0–6px；数字牌、棋子和落点遵循其固有几何。
- 控件：10px（紧凑）、14px（按钮/输入）、18px（容器）；24px 只用于英雄级容器。避免把每个区域做成大圆角白卡。
- 材质：不透明或 94% 白色表面配一层细边框。仅黏性导航允许轻微模糊；游戏盘、弹窗和关键操作不用玻璃材质。
- 阴影：仅使用 `--ui-shadow-sm` / `--ui-shadow-md` 来建立层级。选中使用主色焦点环，不以多层 Glow 代替状态。

## 图标、状态与动效

- 关键操作逐步从 Emoji 收敛为同一套简洁单色图标；Emoji 只保留在非关键的玩法插画或庆祝反馈中。
- 所有可操作元素有 `:focus-visible`；禁用态降低对比但保留文本可读性。
- Hover：最多上移 2px 或改变边框；Pressed：缩放至 0.98；时长 120–180ms。
- 面板/弹窗进入 180–220ms；胜利、倒数和移动路线可更具游戏感，但必须支持 `prefers-reduced-motion`。

## 基础组件契约

`public/ui-foundation.css` 提供 `ui-panel`、`ui-button`、`ui-button--primary`、`ui-button--secondary`、`ui-button--accent`、`ui-button--success`、`ui-button--danger`、`ui-input`、`ui-modal` 和 `ui-toast`。后续迁移时优先扩展这些组件，不复制按钮/面板规则。

- Primary Button：每个操作组只有一个，用于“开始、确认、提交、再来一局”。
- Secondary Button：返回、取消、复制、设置等低优先级操作。
- Danger Button：清除统计、退出房间等不可逆或中断性操作，须配合确认弹窗。
- Panel：用于承载一组相关信息，不能仅为留白而包裹每一项内容。
- Modal / Toast：遮罩层、关闭方式、焦点和语义状态后续统一为基础组件。

## 后续实施顺序

1. **Foundation（已完成）**：已引入共享 Token、通用焦点/减弱动效规则与书面规范；所有入口均接入基础文件。
2. **Primitives（首批已完成）**：已迁移关键导航、主按钮、跳棋输入框、数独弹窗及 Toast 基础样式。下一批继续迁移数独工具栏、24 点对局按钮和各类反馈条；随后移除 24 点内联样式的重复覆盖。
3. **Shared gameplay components**：统一数独数字键盘、24 点数字牌、跳棋回合条/玩家条的状态命名与反馈。
4. **Screens**：优先 24 点联机与结算页，其次数独深色棋局，最后跳棋大厅的卡片化布局。
5. **Visual QA**：每批改动在 1920×1080、1280×720、窄屏（390×844）和宽屏（1440×900）验证，检查文字截断、焦点、禁用、长昵称、房间码和弹窗溢出。
